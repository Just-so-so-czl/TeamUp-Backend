spring:
  application:
    name: TeamUp-backEnd
  datasource:
    url: jdbc:mysql://localhost:3306/TeamUp?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 103119
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-Plus 配置
mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.czl.teamupbackend.model.entity
